export type Address = {
  zip: string;
  address1: string;
  address2: string | '';
};

export type DaumAdddress = {
  address: string;
  autoJibunAddress: string;
  roadAddress: string;
  sigunguCode: string;
};
